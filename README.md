# HotelManagementSystem
Java-based Hotel Management System with SQL Server

# Tạo thư mục và di chuyển vào
mkdir HotelManagementSystem
cd HotelManagementSystem

# Khởi tạo Git repository
git init

# Thêm các file
git add .

# Commit changes
git commit -m "Initial commit: Hotel Management System with Java and SQL Server"

# Thêm remote repository
git remote add origin https://github.com/your-username/HotelManagementSystem.git

# Push lên GitHub
git branch -M main
git push -u origin main

# 1. Clone repository (nếu cần)
git clone https://github.com/your-username/HotelManagementSystem.git
cd HotelManagementSystem

# 2. Tạo cấu trúc thư mục
mkdir -p src/model src/dao src/main database lib

# 3. Copy các file Java vào đúng thư mục
# 4. Copy file SQL vào thư mục database
# 5. Tạo README.md và .gitignore

# 6. Thêm tất cả file
git add .

# 7. Commit
git commit -m "Add complete hotel management system
- Entity classes (Hotel, Room, Customer, Booking)
- DAO classes for database operations  
- Main application with console interface
- SQL database schema
- Documentation and gitignore"

# 8. Push lên GitHub
git push origin main
